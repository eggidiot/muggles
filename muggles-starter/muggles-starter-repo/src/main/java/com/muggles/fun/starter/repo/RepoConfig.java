package com.muggles.fun.starter.repo;

import org.springframework.context.annotation.PropertySource;

/**
 * 默认持久层配置
 */
@PropertySource("classpath:default_repo.properties")
public class RepoConfig {
}
